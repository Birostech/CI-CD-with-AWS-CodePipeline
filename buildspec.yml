version: 0.2

phases:
  build:
    commands:
      - echo "Running tests..."
      - if grep -q "error" index.html; then echo "Test failed!" && exit 1; else echo "Tests passed!"; fi

  post_build:
    commands:
      - echo "Build complete"

artifacts:
  files:
    - '**/*'
  discard-paths: no
